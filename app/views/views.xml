<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="app.list">
      <field name="name">app list</field>
      <field name="model">app.app</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="app.action_window">
      <field name="name">app window</field>
      <field name="res_model">app.app</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="app.action_server">
      <field name="name">app server</field>
      <field name="model_id" ref="model_app_app"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="app" id="app.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="app.menu_1" parent="app.menu_root"/>
    <menuitem name="Menu 2" id="app.menu_2" parent="app.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="app.menu_1_list" parent="app.menu_1"
              action="app.action_window"/>
    <menuitem name="Server to list" id="app" parent="app.menu_2"
              action="app.action_server"/>
    -->
  </data>
</odoo>